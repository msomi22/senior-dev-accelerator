import topic from '../../../banks/dsa/bit-manipulation.js';

const problem = {
  ...topic.questions.find((question) => question.id === 'bit-manipulation-006'),
  category: 'dsa'
};

export default problem;
