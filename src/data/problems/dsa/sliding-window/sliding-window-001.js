import topic from '../../../banks/dsa/sliding-window.js';

const problem = {
  ...topic.questions.find((question) => question.id === 'sliding-window-001'),
  category: 'dsa'
};

export default problem;
